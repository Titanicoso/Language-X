
boolean queueTest() {
  [string] q;
  string a;
  string b;
  string text;
  q = ["Hola","como", "andas"];
  <int> s;
  int c;
  int d;
  s = <1,2, 3>;
  text = "La queue tiene los siguientes datos (haciendo dequeue):"
  for(a:q) {
    b = a
  }
  text = "Y el stack estos otros (haciendo pop):"
  for(c:s) {
    d = c
  }
  return true;
}

int main() {
  queueTest();
}
